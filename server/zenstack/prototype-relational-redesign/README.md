# Throwaway ZenStack prototype

This directory is a scratchpad for validating the proposed ZenStack-first relational redesign.

Question: can a real ZenStack v3 schema express `DocumentBase` polymorphism, inherited policy behavior, and the catalog/scope joins cleanly enough to keep the redesign direction alive?

Run it from the repo root:

```bash
bun run prototype:zenstack-relational-redesign
```

Throwaway marker: nothing in here is production code.
